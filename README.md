# Ozobot Fractions Lab

## Run locally

From this folder, start a local web server:

```bash
python3 -m http.server 8000
```

Then open:

- http://localhost:8000

## Stop server

Press `Ctrl + C` in the terminal where the server is running.
